# test-crate used to check if code generated by openVALIDATION compiles
[package]
name = "ovtest"
version = "0.1.0"
authors = ["Matthias Krueger"]
edition = "2018"
description = "This is a test crate to check if openvalidation rust code compiles"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
